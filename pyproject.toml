[project]
name = "gmail-cli"
version = "1.0.0"
description = "Simple text client to read e-mail from you Gmail account"
readme = "README.md"
requires-python = ">=3.14.7"
dependencies = [
    "google-auth-oauthlib>=1.4.0",
]
